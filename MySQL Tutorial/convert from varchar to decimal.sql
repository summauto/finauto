-- UPDATE overall_screener SET enterpriseValue = REPLACE(enterpriseValue,",","");

-- ALTER TABLE overall_screener
-- MODIFY enterpriseValue 
-- CASE WHEN ISNUMERIC(enterpriseValue)=0 then 0
-- ELSE DECIMAL(15,2);

